<nav id="primary-menu" class="style-5 style-6" style="text-shadow: 3px 3px 3px rgba(0,0,0,0.2)!important;">
    <ul>
        <li><a href="{{path('trip_new')}}"><div><i class="icon-paper-plane"></i>Créer un groupe de voyage</div></a>
        <li><a href="{{path('ad_new')}}"><div><i class="icon-line2-home"></i></i>Accueillir des voyageuses</div></a></li>
        <li><a href="#"><div><i class="icon-pencil2"></i>Blog</div></a></li>
        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
            <i class="icon-caret-down" style="font-size: 15px; color:#4897d8!important;"></i>
            <li>
                <a href="#">
                    <div ><i class="icon-user2"></i>Bienvenue {{app.user.firstName}} </div>
                </a>
                <ul>
                    <li><a href="#"><div><i class="icon-dashboard"></i>Ton tableau de bord</div></a></li>
                    <li><a href="#"><div><i class="icon-line2-plane"></i>Tes voyages</div></a></li>
                    <li><a href="#"><div><i class="icon-line2-home"></i>Tes locations</div></a></li>
                    <li><a href="{{path('security_logout')}}"><div><i class="icon-line2-logout"></i>Déconnexion</div></a></li>
                </ul>
            </li>
                {# {{format_avatar(app.user)}} #}
        {% else %}
            <li class="bgcolor">
            	<a href="{{path('security_login')}}">
            		<div>
            			<i class="icon-users2"></i>
            			Se connecter
            		</div>
            	</a>
            </li>
            <li>
                <a href="{{path('security_register')}}">
            	    <div>
            			<i class="icon-line-file-add"></i>
            			S'inscrire
            		</div>
            	</a>
            </li>

       {% endif %}
    </ul>
</nav><!-- #primary-menu end -->
{% for label, messages in app.flashes %}

    {% for message in messages %}
        <a href="#" class="btn btn-{{label}}" data-notify-type="{{label}}" data-notify-position="top-full-width " data-notify-close=true data-notify-msg="<i class=icon-info-sign></i> {{message}}" onclick="SEMICOLON.widget.notifications(this); return false;">{{message}}</a>
    {% endfor %}

{% endfor %}

