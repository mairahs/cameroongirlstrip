
    <div id="posts" class="events small-thumbs">
        {% for comment in adComment %}
			<div class="entry clearfix">
				<div class="entry-image">
             
					<a href="#">
						{% if comment.author.fileName %}
                        	<img style="width:200px!important; height:200px!important;" class="alignleft img-circle img-thumbnail nomargin image_fade testi-image" src="{{vich_uploader_asset(comment.author, 'avatarFile') | imagine_filter('thumb')}}" alt="Avatar de {{comment.author.fullName}}">             
					    {% else %}
                            <img style="width:200px!important; height:200px!important;" class="alignleft img-circle img-thumbnail nomargin image_fade testi-image" src="/images/img/avatardefault.jpg" alt="Avatar par défaut de {{comment.author.fullName}}">
					    {% endif %} 
                    </a>
                </div>    
				<div class="entry-c">
					<div class="entry-title">
						<h2><a href="{{path('ad_view', {'slug':comment.ad.slug})}}"><i class="icon-line-move"></i> Logement {{comment.ad.title}} situé à {{comment.ad.location}}</a></h2>
					</ul></a></h2>
                        <div class="entry-content">
                            <blockquote style:"text-align:justify;">{{comment.content|nl2br}}</blockquote>
                        </div>
						<p style:"text-align:center; font-weight:bold;">{% include 'inc/rating.html.twig' with {'rating':comment.ad.avgRatings}  %}</p>		
					</div>
					<ul class="entry-meta clearfix">
						<li><span style="font-weight:bold; font-size:15px; font-family:'Cabin Sketch', cursive; color:#333;"><i class="icon-user4"></i> {{comment.author.fullName}}</span></li>
						<li><span style="font-weight:bold; font-size:15px; font-family:'Cabin Sketch', cursive; color:#333;"><i class="icon-calendar3"></i> le {{format_created_at_ad_comment(comment)}}</span></li>
					</ul>
				</div>
			</div>
        {% endfor %}
	</div> 

       

