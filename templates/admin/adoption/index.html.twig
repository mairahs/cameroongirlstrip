{% extends 'admin/base.html.twig' %}
{% block title %} Administration des options sur les logements| {{parent()}}{% endblock %}

{% block body %}
 <div class="container">
		<h1 class="my-5 text-center">Gestion des {{adOptions|length}} options sur les logements</h1>
		<table class="table table-hover" >
			<thead>
				<tr>
					<th>Id</th>
					<th>Nom</th>
					<th>Actions</th>
				</tr>
			</thead>
			<tbody>
				{% for adOption in adOptions %}
				<tr>
					<td>{{adOption.id}}</td>
					<td>{{adOption.name}}</td>
					<td>
						<a href="{{path('adminadoption_edit', {'id':adOption.id})}}" class="btn btn-warning"><i class="icon-edit2"></i></a> 
						<a href="{{path('adminadoption_delete', {'id':adOption.id})}}" class="btn btn-danger"><i class="icon-trash-alt1"></i></a>
					</td>
				</tr>
			{% endfor %}
			</tbody>
		</table>
	</div> 
{% endblock %}
